# Paper Review Benchmark

A Paper Review benchmark adapted from `hyperagents`. Prompts are based on the `meta-hyperagents` paper_review domain (which was itself originally adapted from SakanaAI's AI-Scientist `perform_review.py`).

This repository is primarily used for research on self-improving AI applications and CodeEvolver.
